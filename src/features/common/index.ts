import {
  Center,
  VCenter,
  HCenter,
  Input,
  SuspenseHOC,
  ErrorBoundaryHOC,
} from "./components";
import { useForm, useApi } from "./hooks";

export const Common = {
  SuspenseHOC,
  ErrorBoundaryHOC,
  Center,
  VCenter,
  HCenter,
  Input,
  useForm,
  useApi,
};
